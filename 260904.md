### B-Tree 인덱스 기본 구조
- B-Tree = Balanced Tree의 약자로, RDBMS에서 가장 널리 쓰이는 표준 인덱스 자료구조
- 트리 구조 = 루트(Root) 노드 -> 브랜치(Branch) 노드 -> 리프(Leaf) 노드의 계층 구조를 가짐
- 루트 및 브랜치 노드 = 하위 자식 노드를 찾아가기 위한 기준값과 자식 노드의 물리적 주소를 가짐
- 리프(Leaf) 노드 = 실제 인덱스 키 값과 해당 레코드를 찾아갈 수 있는 주소값(또는 PK)을 가짐
- 탐색 방식(Tree Traversal) = 루트에서 시작해 부등호 조건에 따라 알맞은 자식 노드를 타고 내려가 리프 노드에 도달함
- 정렬 상태 유지 = 인덱스의 키 값은 삽입/수정 시 항상 정렬된 상태를 유지함
- 검색 성능 = 항상 정렬되어 있으므로 동등(`=`) 조건뿐만 아니라 부등호(`<`, `>`)나 `BETWEEN` 같은 범위 검색에 매우 유리함
- 쓰기 성능 저하(Trade-off) = `INSERT`, `UPDATE`, `DELETE` 발생 시 인덱스 트리도 함께 갱신하고 재정렬해야 하므로 쓰기 작업 시 오버헤드 발생

### 클러스터형(Clustered) vs 비클러스터형(Non-Clustered) 인덱스
- 클러스터형 인덱스 = 테이블당 오직 1개만 존재 가능하며, 보통 기본키(PK)에 자동으로 생성됨
- 클러스터형의 특징 = 리프 노드 자체가 실제 데이터 레코드(행)를 통째로 품고 있음 (사전과 같은 구조)
- 물리적 정렬 = 데이터가 클러스터형 인덱스 키값 순서대로 물리적(디스크 상)으로 정렬되어 저장됨
- 비클러스터형 인덱스 = 세컨더리 인덱스(Secondary Index)라고도 부르며, 한 테이블에 여러 개 생성 가능 (일반 서적의 맨 뒤 '찾아보기'와 같음)
- 비클러스터형의 특징 = 리프 노드에 실제 데이터가 아닌 'PK 값(또는 레코드 주소)'만 저장됨
- 탐색 과정(RID Lookup) = 세컨더리 인덱스 탐색 -> 리프 노드에서 PK 확인 -> 해당 PK로 클러스터형 인덱스를 다시 찔러서 실제 데이터 확보
- 커버링 인덱스(Covering Index) = `SELECT` 절에 필요한 모든 컬럼이 이미 세컨더리 인덱스에 포함되어 있어, 본 테이블(PK 탐색)까지 안 가고 인덱스만 읽고 끝내는 초고속 조회 패턴

### 복합 인덱스(Composite Index)와 설계 제약
- 복합 인덱스 = 2개 이상의 컬럼을 묶어서 생성한 하나의 인덱스
- 선두 컬럼의 절대성 = 인덱스는 설정된 컬럼 순서대로 정렬됨 (1번 컬럼 정렬 후, 1번 값이 같은 애들끼리 2번 컬럼 정렬)
- 선두 컬럼 누락 시 = `WHERE` 조건에 복합 인덱스의 선두 컬럼이 없으면 인덱스를 전혀 타지 못하거나 효율이 극감함 (풀 스캔 발생)
- 컬럼 순서 배치 전략 1 = `WHERE` 절에서 자주, 그리고 같이 조회되는 컬럼들을 묶음
- 컬럼 순서 배치 전략 2 = 카디널리티(Cardinality, 값의 종류가 많고 중복도가 낮음)가 높은 컬럼을 앞쪽에 두는 것이 선택도를 높여 탐색 효율을 극대화함
- 점 조건 vs 선 조건 = `=`이나 `IN` 같은 점(Point) 조건을 앞쪽에, 부등호나 `BETWEEN`, `LIKE` 같은 선(Range) 조건을 뒷쪽에 배치해야 탐색 범위를 효과적으로 줄임

### 인덱스 무력화 패턴 (SARGable 위반)
- SARGable = Search Argument Able의 약자, 인덱스를 정상적으로 활용할 수 있는 검색 조건을 의미
- 컬럼 가공 = `WHERE YEAR(created_at) = 2026` -> 좌변(인덱스 컬럼)을 함수로 훼손하면 인덱스 트리 탐색 불가. `WHERE created_at >= '2026-01-01'` 형태로 우변을 가공해야 함
- 묵시적 형변환 = 문자열(`VARCHAR`) 컬럼에 숫자 타입을 대입하면, DB 엔진이 내부적으로 숫자 형태로 캐스팅하느라 컬럼이 가공되어 인덱스 사용 불가
- 부정형 조건 = `!=`, `<>`, `NOT IN` 등은 "조건이 아닌 것"을 찾기 위해 결국 전체 데이터를 뒤져야 하므로 인덱스를 타기 어려움
- 좌측 와일드카드 = `LIKE '%검색어'` 패턴은 문자열의 시작점을 알 수 없어 B-Tree 탐색(앞에서부터 비교)이 원천적으로 불가능하여 테이블 풀 스캔 발생
- IS NULL 처리 = 과거에는 NULL이 인덱스에 빠져 안 탔으나, 최근 엔진(InnoDB 등)은 NULL도 인덱스에 저장하므로 경우에 따라 인덱스 스캔 가능

### 실행 계획(EXPLAIN) 핵심 지표 읽기
- EXPLAIN = 옵티마이저가 해당 쿼리를 어떻게 실행할지(어떤 인덱스를 타고, 어떻게 조인할지) 미리 짠 계획표를 보여주는 명령어
- id = 쿼리 내의 `SELECT` 실행 순서. 조인 시에는 같은 id가 부여되고, 서브쿼리가 복잡할수록 고유한 id 값이 늘어남
- table = 실행 계획의 각 단계에서 접근하고 있는 물리적 테이블명 또는 파생 테이블(Alias) 명
- type (접근 방식) = 데이터를 어떻게 읽는지 나타내는 가장 중요한 튜닝 지표. (성능 좋음 -> 나쁨 순서로 배열)
- type : `system` / `const` = PK나 유니크 인덱스로 정확히 단 1건만 바로 찾는 최고 속도
- type : `eq_ref` = JOIN 상황에서, 후행(Driven) 테이블에서 PK나 유니크 인덱스로 1건씩 정확히 매칭될 때
- type : `ref` = 유니크하지 않은 일반 세컨더리 인덱스로 동등(`=`) 검색을 수행할 때
- type : `range` = 인덱스를 이용해 특정 범위(`<`, `>`, `BETWEEN`, `IN`)를 검색할 때
- type : `index` = 인덱스 풀 스캔. 인덱스 트리의 처음부터 끝까지 다 훑는 방식 (데이터보다는 낫지만 성능 주의 필요)
- type : `ALL` = 테이블 풀 스캔. 인덱스를 안 타고 디스크에서 테이블의 처음부터 끝까지 다 읽음 (가장 피해야 할 튜닝 1순위)
- possible_keys = 옵티마이저가 쿼리 실행을 위해 사용을 고려해 본 후보 인덱스 목록
- key = 옵티마이저가 비용(Cost)을 계산한 후 최종적으로 선택해서 실제 사용하기로 결정한 인덱스
- rows = 쿼리 결과를 찾기 위해 DB 엔진이 읽어야 할 것으로 예상되는 대략적인 데이터 행(Row) 수 (적을수록 최적화된 것)
- Extra = 쿼리 실행 시 엔진 내부에서 발생하는 부가적인 처리 작업 정보
- Extra : `Using index` = 커버링 인덱스 사용됨. 실제 데이터 레코드까지 안 가고 인덱스 리프 노드만 읽고 쿼리가 끝남 (가장 이상적)
- Extra : `Using filesort` = 인덱스를 이용한 정렬이 불가능해서, DB 엔진이 메모리(또는 디스크)에 데이터를 올려 직접 정렬함 (튜닝 대상)
- Extra : `Using temporary` = `GROUP BY`, `ORDER BY`, `DISTINCT` 등을 처리하기 위해 내부적으로 임시 테이블을 생성함 (무거우므로 튜닝 대상)
- Extra : `Using where` = 스토리지 엔진에서 던져준 데이터를 MySQL(또는 DB) 엔진 레벨에서 `WHERE` 조건으로 한 번 더 필터링을 거쳤음을 의미