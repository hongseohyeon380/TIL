### Spring AI 컨텍스트(ChatHistory) 

- **정의** : LLM(대규모 언어 모델)은 기본적으로 상태를 저장하지 않는(Stateless) 특성을 가지므로 이전 대화의 맥락(Context)을 유지하기 위해 프레임워크 단에서 과거 대화 기록을 관리하고 프롬프트에 주입하는 아키텍처 기술
- **LLM은 기억력이 없다.** 질문을 보낼 때마다 '과거의 대화 내역'을 프롬프트에 함께 포장해서 보내야 하며 Spring AI는 이 포장 작업을 추상화하여 제공

---

### ChatClient vs ChatMemory vs ChatHistory

| 구성 요소 | 정의 및 역할 | 메타포 | 주요 특징 |
| :--- | :--- | :--- | :--- |
| **ChatClient** | LLM 요청을 생성하고 실행하는 **고수준 Fluent API (실행 창구)** | **대화 주체 (말하는 사람)** | • 프롬프트 구성, Advisor 부착, AI 모델 호출을 담당<br>• 메모리(ChatMemory)를 어드바이저로 부착하여 사용 |
| **ChatMemory** | 대화 기록을 저장·조회·삭제하는 **메모리 관리 인터페이스 (전략)** | **기억 장치 (뇌/저장소)** | • `add()`, `get()`, `clear()` 메서드로 저장소 인터페이스 제공<br>• `InMemoryChatMemory`, `VectorStoreChatMemory` 등 구현체 존재 |
| **ChatHistory** | 실제 저장소에 축적된 **과거 대화 메시지 데이터 (실체)** | **기록된 일기장 (데이터)** | • `List<Message>` 형태로 수집되는 도메인/개념적 데이터<br>• 토큰 제한이나 슬라이싱 정책의 대상이 되는 실제 내용물 |

---

### Spring AI 컨텍스트 관리의 3대 핵심 요소
- **ChatMemory (인터페이스)** : 과거의 대화 기록(ChatHistory)을 저장(`add`), 조회(`get`), 삭제(`clear`)하는 추상화 레이어
- **Message (메시지 객체)** : 대화의 주체와 역할을 구분하는 표준 규격 (`SystemMessage`, `UserMessage`, `AssistantMessage` 등)
- **Advisor (어드바이저)** : `ChatClient` 호출 파이프라인을 가로채어(Intercept) `ChatMemory`에서 과거 기록(`ChatHistory`)을 꺼내 프롬프트에 주입하고 LLM의 새로운 응답을 다시 메모리에 저장하는 중개자

---

### 방식 1. Advisor 기반 자동 관리 (선언적 방식)
- **작동 원리** : `ChatClient`에 `MessageChatMemoryAdvisor`를 부착하여 컨텍스트의 **조회-병합-저장 루프를 자동화**하는 방식
  1. **조회** : 사용자의 새 요청이 들어오면 세션(Conversation ID)을 기준으로 `ChatMemory`에서 과거 `ChatHistory`를 꺼냄
  2. **병합** : 과거 대화와 현재 사용자의 질문을 하나의 프롬프트로 병합하여 `ChatClient`가 LLM에 전송
  3. **저장** : LLM의 답변이 돌아오면, 현재 턴의 질문과 답변을 묶어 다시 `ChatMemory`에 자동 저장
- **특징** : 비즈니스 로직(AI 호출)과 인프라 로직(기억 관리)을 완벽히 분리하여 코드 결합도를 낮춤

---

### 방식 2. Message 리스트 수동 제어 (명령형 방식)
- **작동 원리** : 개발자가 직접 영구 저장소(DB)에서 과거 기록(`ChatHistory`)을 불러와 `List<Message>` 형태로 조립한 뒤, 프롬프트에 담아 `ChatClient` 또는 `ChatModel`로 전송하는 방식
- **특징** : 프레임워크의 자동화 루프에 의존하지 않고 개발자가 모든 과정을 통제
- **적용 환경** :
  - 특정 시점의 대화만 선별적으로 주입해야 할 때
  - 비즈니스 규칙에 따라 과거 대화 내용을 동적으로 변형하거나 마스킹(Masking)해야 할 때

---

### 프로덕션(운영) 환경에서의 주요 고려 사항
- **상태의 영속성 (Persistence)** : Spring AI가 기본 제공하는 메모리 구현체(`InMemoryChatMemory`)는 서버 재시작 시 휘발됨. 실제 서비스에서는 RDB(JPA), Redis 등과 연동되는 커스텀 `ChatMemory` 구현체를 개발하여 빈(Bean)으로 등록해야 함
- **컨텍스트 윈도우 관리 (비용 및 지연 시간)** : 과거 대화(`ChatHistory`)를 무한정 누적하면 LLM의 토큰 한도를 초과하고, API 비용 급증 및 응답 지연(Latency)이 발생함
- **해결 전략 (토큰 최적화)** :
  - **윈도우 슬라이싱 (Window Slicing)** : 조회 크기(retrievalSize)를 제한하여 최근 N개의 메시지만 프롬프트에 포함. (예: `MessageWindowChatMemory`)
  - **컨텍스트 압축 (Context Compaction)** : 오래된 대화는 별도의 요약 프롬프트를 통해 하나의 요약된 시스템 메시지(System Message)로 압축하여 보존