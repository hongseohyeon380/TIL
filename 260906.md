### 데이터 저장 구조 및 I/O 메커니즘
- SQL이 느린 이유 = 디스크 I/O (디스크에서 데이터를 읽고 쓰는 작업이 물리적으로 가장 느리기 때문)
- 프로세스 대기 = OS 또는 I/O 서브 시스템이 I/O를 처리하는 동안 DB 프로세스는 CPU를 반환하고 sleep(waiting) 상태에 빠짐
- 데이터베이스 논리적 저장 구조 = 테이블스페이스 > 세그먼트 > 익스텐트 > 블록
  - 블록(Block) = 데이터를 읽고 쓰는 기본 논리적 단위 (일반적으로 8KB)
  - 익스텐트(Extent) = 공간을 확장하는 단위 (서로 인접한 연속된 블록의 집합)
  - 세그먼트(Segment) = 데이터 저장공간이 필요한 물리적 오브젝트 (테이블, 인덱스 등)
  - 테이블스페이스(Tablespace) = 세그먼트를 담는 논리적 컨테이너 (물리적인 데이터파일로 구성됨)
- 데이터파일 = 디스크 상의 물리적인 OS 파일
- 공간의 연속성 = 익스텐트 내의 블록들은 서로 인접한 연속된 공간이지만, 익스텐트끼리는 디스크 상에서 연속된 공간이 아닐 수 있음
- DBA (Data Block Address) = 데이터 블록이 디스크 상에 위치한 고유 주소값 (데이터를 찾아갈 때 이 주소를 참조)
- 시퀀셜 액세스(Sequential Access) = 논리적 또는 물리적으로 연결된 순서에 따라 차례대로 연속해서 블록을 읽는 방식
  - 특징 = 오라클은 세그먼트 헤더에 익스텐트 맵을 관리하며, 이를 통해 읽어야 할 익스텐트 목록을 확인하고 연속적으로 스캔함 (테이블 풀 스캔 시 유리)
- 랜덤 액세스(Random Access) = 논리적, 물리적인 순서와 무관하게 레코드 하나를 읽기 위해 블록을 건건이 찾아가는 방식
  - 특징 = 인덱스 리프 노드에서 주소값(ROWID)을 얻어 테이블 블록을 찾아갈 때 주로 발생하며, 대량 데이터 조회 시 성능 저하의 주범

### 논리적 I/O와 물리적 I/O
- DB 버퍼 캐시 (Buffer Cache) = 디스크 I/O의 속도 문제를 해결하기 위해 자주 읽는 데이터 블록을 메모리에 캐싱해 두는 공간 (SGA 소속)
- 논리적 I/O = SQL 쿼리를 처리하기 위해 메모리(버퍼 캐시)에서 읽고 쓴 총 블록 I/O 횟수 (해당 쿼리가 일한 총량)
- 물리적 I/O = 버퍼 캐시에서 블록을 찾지 못해(Cache Miss) 결국 디스크에서 직접 읽어온 블록 I/O 횟수 (SQL이 느려지는 직접적 원인)
- BCHR (Buffer Cache Hit Ratio) = 버퍼 캐시 효율을 나타내는 지표 (전체 읽은 블록 중 메모리에서 찾은 비율)
- Single Block I/O = 한 번의 I/O 호출로 디스크에서 하나의 블록만 메모리로 적재하는 방식 (인덱스 탐색 등 랜덤 액세스 발생 시 짝꿍으로 동작)
- Multi Block I/O = 한 번의 I/O 호출로 디스크에서 인접한 여러 블록(익스텐트 단위 등)을 한 번에 메모리로 적재하는 방식 (테이블 풀 스캔 등 시퀀셜 액세스 발생 시 짝꿍으로 동작)
- SQL 튜닝의 본질 = 물리적 I/O를 줄이려면 결국 논리적 I/O(메모리 읽기 횟수) 자체를 줄여야 함
- 해결 방안 = 불필요한 인덱스 랜덤 액세스를 줄이고, 최적의 인덱스를 설계하거나 조인 방식을 개선하여 읽어야 할 총 블록 수를 최소화하는 것

### Single Block I/O vs Multi Block I/O
- I/O Call = DB 프로세스가 OS에 디스크 데이터를 읽어달라고 요청하는 행위 (시스템 자원을 많이 쓰는 무거운 작업)
- Single Block I/O = 한 번의 I/O Call로 디스크에서 단 하나의 블록만 읽어 메모리(버퍼 캐시)에 적재하는 방식
  - 동작 환경 = 주로 인덱스를 통한 랜덤 액세스 시 발생 (인덱스 루트 -> 브랜치 -> 리프 -> 테이블 레코드 이동 시 각각 한 블록씩 읽음)
  - 단점 = 대량의 데이터를 읽을 때 매번 I/O Call이 발생하므로 심각한 성능 저하의 원인이 됨
- Multi Block I/O = 한 번의 I/O Call로 디스크에서 인접한 여러 블록(하나의 익스텐트 범위 내)을 한 번에 읽어 메모리에 적재하는 방식
  - 동작 환경 = 테이블 풀 스캔(Full Table Scan)이나 인덱스 패스트 풀 스캔(Index Fast Full Scan) 등 시퀀셜 액세스 시 발생
  - 장점 = I/O Call 횟수를 획기적으로 줄여주므로 대용량 데이터 스캔에 훨씬 효율적임 (한 번에 읽는 블록 수는 파라미터로 설정됨)
- "인덱스를 타면 무조건 빠르다"는 것은 착각. 대량의 데이터를 Single Block I/O로 건건이 퍼 올리는 것보다 차라리 Multi Block I/O로 한 번에 쓸어 담는 것이 빠를 수 있음
- 손익분기점 (Index vs Full Scan) = 읽어야 할 데이터량이 전체의 특정 비율(일반적으로 10~20%)을 넘어가면 인덱스 랜덤 액세스보다 테이블 풀 스캔이 더 유리해짐