### HTTP 기본 인증
- 클라이언트가 서버의 보안 영역인 렐름(realm)에 접근하기 위해 사용자 ID, PW 한 쌍을 Base64 방식으로 인코딩 한 자격 증명을 서버로 전달
- Authorization: Basic Base64({USERNAME}:{PASSWORD})
- 실행 흐름
  - [Client] -> [Server] : 인증 정보 없이 요청
  - [Client] <- [Server] : 서버는 인증 정보가 없으므로 401 unauthorized1` 상태 코드 반환
    - WWW-Authenticate 헤더에 인증 타입과 렐름 정보를 함께 전달 (인증에 대한 힌트 제공)
  - [Client] -> [Server] : WWW-Authenticate 헤더 확인 후 유효 인증 정보 함께 전달
  - [Client] <- [Server] : 접근 권한 있는 경우 200, 없는 경우 403 반환 
```java
// HTTP 기본 인증 디폴트 설정
httpSecurity.httpBasic(
    Customizer.withDefaults()
);
```
- 디폴트 설정을 사용하면 2개의 인증 필터가 추가 됨
  - `BasicAuthenticationFilter` : HTTP 기본 인증 수행
  - `CorsFilter` : CORS 정책 관련 처리 수행

### `BasicAuthenticationFilter`
- 인증에 필요한 정보를 담은 Authentication 인스턴스를 만듦
  - HTTP 헤더 Authorization 키에 매칭된 자격 증명을 Base64 디코딩
  - 디코딩을 통해 얻은 아이디와 비밀번호로 UsernamePasswordAuthenticationToken 객체를 생성
- 인증 매니저에 의해 인증 작업 완료
- 인증 성공 여부 확인 
  - 성공한 경우
    - 빈 SecurityContext 인스턴스를 만들어 내부에 Authentication 인스턴스를 저장
    - SecurityContext 인스턴스를 SecurityContextHolder 클래스에 보관
    - 리멤버-미 인증이 활성화 되어 있다면 이를 위한 부가적인 작업 처리
    - 필터체인의 다음 필터 실행
  - 실패한 경우
    - SecurityContextHolder 클래스 정리
    - 리멤버-미 인증에 사용되는 정보 정리
    - AuthenticationEntryPoint 인스턴스가 401 에러 코드와 WWW-Authenticate 헤더를 응답
  
### 폼 로그인 인증과 HTTP 기본 인증의 차이점
- 인증 조건
  - 폼로그인 : 특정 URL 경로로 POST 요청이 들어오면 인증 작업 수행
  - HTTP 기본 인증 : Authorization 헤더에 Basic 타입 자격 증명이 있는 경우 인증 작업 수행 
- 리다이렉트 여부
  - 폼로그인 : 성공 여부와 상관 없이 클라이언트 리다이렉트
  - HTTP 기본 인증 : 성공한 경우 필터 체인 계속 진행, 실패한 경우 특정 플래그에 따라 필터 진행하거나 프로세스 종료
- 세션 사용 여부 
  - 폼로그인 : 인증 성공 시 세션에 인증 정보 저장, 클라이언트는 쿠키를 사용해 인증된 사용자로서 동작
  - HTTP 기본 인증 : 인증 정보 별도로 저장 X, 클라이언트는 요청마다 인증을 위한 자격 증명을 Authorization 헤더에 담아 보내야함

### 예외처리
- BasicAuthencitacionFilter 객체에서 HTTP 기본 인증에 실패하거나 ExceptionTranslationFilter 객체 이후에 예외가 발생하면 BasicAuthenticationEntryPoint 객체에 예외 응답을 만드는 작업을 위임
  - 인증되지 않은 사용자의 요청인 경우 BasicAuthenticationEntryPoint 객체에 401 상태 코드와 WWW-Authenticate 해더 설정을 위임
  - 인증되었지만 권한이 적절하지 않은 경우 별도 핸들러를 통해 403 상태 코드를 응답