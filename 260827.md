### Optional
- 값이 없는 경우를 표현하기 위한 용도
- 제네릭을 사용하여 만들어짐 -> 어느 타입의 객체이든 상관 없음
- 객체 값이 null인 경우 null 체크 없이 메서드 호출할 경우 NPE 발생 -> 다라서 optional을 사용
  - 빈 결과를 명확하게 반환 가능
  - null을 반환하는 메서드보다 오류 가능성이 적음
  - null일 때 예외를 던지는 메서드 보다 더 사용이 용이한 코드 만들기 가능
- Optional 생성 방법
  - `Optional.empty()` : 값이 비어있는 Optional 생성
  - `Optional.of("***")` : "***" 값이 담겨있는 Optional 생성 (Null 불가능)
  - `Optional.ofNullable(temp)` : temp가 null일 수도 있는 상황에서 사용
- Optioanl 제공 메서드
  - `isPresent()` : 값 O -> true 값 X -> false
  - `isEmpty()` : 값 X -> true 값 O -> false
  - `get()` : Optional이 감싸고 있는 실제 값 얻기 위해 사용 
    - 만약 Optional 값이 비어있는 경우 -> `NoSuchElementException`
    - 따라서 `isPresent()`, `isEmpty()`로 존재 여부 체크 필요
    - `ifPresent()` : 값이 있는 경우 처리 할 작업
    - `ifPresentOrElse()` : 값이 있는 경우와 없는 경우 할 작업
    - `orElse()` : empty인 경우 대체값 지정
    - `orElseGet()` : empty인 경우 대체값을 람다 함수로 지정
    - `orElseThrow()` : emtpy인 경우 예외 던지기
    - `or()` : Optional 객체 리턴 가능 (Java 9)
    - `filter()` : 조건부 처리
    - `map()` : Optional의 값을 변환해서 사용해야하는 경우 기존 값을 변환하여 값을 가져옴
    - `flatMap()` : `map()`과 동일하지만 객체를 감싸는 Optional객체를 리턴함 
  - Java Optional은 실제 값을 한번 감싸서 사용하기 때문에 사용하지 않을 때 보다 처리시간이 더 많이 소요될 수 있다
  - 주의사항
    - Optional 변수에 null 할당 X (Optional은 null을 사용하지 않기 위해 만든 클래스, 빈 값 `Optional.empty()`)
    - Optional을 필드 타입으로 사용 X (메서드의 리턴 타입으로 사용하기 위한 용도, 필드 타입은 객체 그대로 사용하는것이 올바르다)
    - 메서드의 인자로 Optional X (메서드의 리턴 타입으로 사용하기 위한 용도, 메서드 인자 또한 객체 그대로 사용하는 것이 올바르다)
    - 빈 컬렉션이나 배열을 나타낼 때 Optional X (빈 컬렉션을 리턴하는 것이 올바르다)
    - 원시 타입을 값으로 가지는 Optional이 필요할 때는 원시 타입 용도로 만들어진 클래스 사용 (`OptionalInt`, `OptionalLong`, `OptionalDouble`)

```java
class Address {
    private String city;
    public String getCity() { return city; }
}

class User {
    private Address address;
    public Address getAddress() { return address; }
}

public class UserService {
    
    // 이 메서드를 Optional을 사용하여 리팩토링 하세요.
    public String getUserCity(User user) {
        if (user != null) {
            Address address = user.getAddress();
            if (address != null) {
                String city = address.getCity();
                if (city != null) {
                    return city;
                }
            }
        }
        return "Unknown City";
    }
}
```

```java
public class UserService {
    public String getUserCity(User user){
        return Optional.ofNullable(user)
            .map(User::getAddress)
            .map(Address::getCity)
            .orElse("Unknown City");
    }
}
```