### SQL 논리적 실행 순서
- 작성 순서 = `SELECT` -> `FROM` -> `WHERE` -> `GROUP BY` -> `HAVING` -> `ORDER BY`
- 실제 실행 순서 = `FROM` -> `WHERE` -> `GROUP BY` -> `HAVING` -> `SELECT` -> `ORDER BY`
- `FROM` (+ `JOIN`) = 데이터를 가져올 대상 테이블 확인 및 조인 수행
- `WHERE` = 가져온 데이터 중 조건에 맞지 않는 행을 필터링 (최대한 여기서 걸러야 성능 향상)
- `GROUP BY` = 조건에 맞는 데이터들을 특정 컬럼 기준으로 그룹화
- `HAVING` = 그룹화된 결과에서 조건에 맞지 않는 그룹 필터링 (`WHERE`와 달리 그룹 집계 함수 사용 가능)
- `SELECT` = 최종적으로 출력할 컬럼 선택 및 가공 (이 단계에서 비로소 별칭(Alias) 생성 및 인식)
- `ORDER BY` = 추출된 데이터를 정렬 (가장 마지막에 실행되므로 `SELECT`에서 만든 Alias 사용 가능)
- 주의사항 = `WHERE` 절에서는 `SELECT` 절에서 지정한 별칭(Alias) 사용 불가 (실행 순서가 더 빠르기 때문)

### JOIN 동작과 필터링 시점
- `INNER JOIN` = 교집합만 반환하므로 조건 필터링을 `ON` 절에 하든 `WHERE` 절에 하든 결과와 성능 동일 (옵티마이저가 최적화)
- 권장사항 = 가독성을 위해 테이블 간의 연결 조건은 `ON` 절에, 일반 데이터 필터링 조건은 `WHERE` 절에 작성
- `OUTER JOIN` = 기준 테이블의 데이터를 유지해야 하므로 `ON`과 `WHERE`의 필터링 시점이 매우 중요함
- `OUTER JOIN`의 `ON` 조건 = 조인 대상을 찾을 때만 적용됨 (매칭 안 돼도 기준 테이블 데이터는 NULL과 함께 보존)
- `OUTER JOIN`의 `WHERE` 조건 = 조인이 완전히 끝난 후 병합된 최종 결과 셋에서 필터링 수행
- 주의사항 = `LEFT JOIN` 사용 시 우측 테이블의 조건을 `WHERE`에 쓰면 사실상 `INNER JOIN`처럼 동작하여 기준 데이터가 통째로 날아갈 수 있음
- NL Join (Nested Loops) = 이중 for문 방식의 조인. 소량 데이터나 조인 컬럼에 인덱스가 있을 때 유리함
- Hash Join = 한쪽 데이터를 메모리에 올려 해시 테이블을 생성하여 조인. 대용량 비인덱스 데이터 처리에 유리함
- Sort Merge Join = 양쪽 테이블을 정렬한 후 병합. 최근에는 주로 Hash Join으로 대체되는 추세

### 서브 쿼리의 형태와 특성
- 인라인 뷰 (Inline View) = `FROM` 절에 위치하는 서브 쿼리. 쿼리 실행 중에만 존재하는 일회성 가상 테이블 역할
- 스칼라 서브 쿼리 (Scalar Subquery) = `SELECT` 절에 위치하는 서브 쿼리. 반드시 단일 행, 단일 컬럼의 값 1개만 반환해야 함
- 중첩 서브 쿼리 (Nested Subquery) = `WHERE` 절이나 `HAVING` 절에 위치하는 서브 쿼리. 주로 `IN`, `EXISTS` 등과 함께 사용됨
- 비상관 서브 쿼리 (Uncorrelated Subquery) = 메인 쿼리와 독립적으로 실행 가능. 서브 쿼리가 먼저 단독 실행된 후 메인 쿼리에 결과 제공
- 상관 서브 쿼리 (Correlated Subquery) = 서브 쿼리 내부에 메인 쿼리의 컬럼이 포함됨. 메인 쿼리의 각 행마다 서브 쿼리가 매번 새로 실행되어 성능 저하 유발 가능
- 서브 쿼리의 단점 = 가상의 뷰를 생성하므로 물리적 인덱스 활용이 제한적이고, 임시 메모리 사용량이 늘어날 수 있음
- 서브 쿼리 최적화 (Unnesting) = 똑똑한 최신 DB 옵티마이저는 가능한 한 서브 쿼리를 내부적으로 JOIN 형태로 풀어서 실행함
- 튜닝의 핵심 = 무분별하게 작성된 복잡한 상관 서브 쿼리는 가급적 명시적인 JOIN 형태로 변환(Rewrite)하는 것이 성능에 좋음