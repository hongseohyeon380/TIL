### 스트림 중간 연산자
- 기존 스트림을 가공하여 새로운 스트림을 반환하는 연산자
- 중간 연산자는 여러 개를 체이닝하여 사용할 수 있으며 최종 연산자가 호출될 때 까지 실제 연산이 수행되지 않는 지연 평가 특성을 가진다
- 스트림 자르기
  - 스트림의 일부 요소만 취하거나 제외
  - `limit(long maxSize)`
    - 스트림의 앞에서부터 maxSize만큼의 요소만 남기고 나머지는 버림
    - `stream.limit(3)` (상위 3개만 추출)
  - `skip(long n)`
    - 스트림의 처음부터 n개의 요소를 건너뛰고 나머지 요소로 구성된 스트림을 반환
    - stream.skip(2) (처음 2개 제외)
- 스트림 요소 걸러내기
  - 조건에 맞는 요소를 걸러내거나 중복을 제거
  - `filter(Predicate<T>)`
    - 주어진 조건(Predicate)이 true인 요소만 추출합니다.
    - `stream.filter(x -> x > 5)`
  - `distinct()`
    - 스트림 내의 중복된 요소를 제거합니다. (객체의 `equals()`와 `hashCode()` 기준)
    - `stream.distinct()`
- 정렬
  - 스트림의 요소를 지정된 기준에 따라 정렬
    - `sorted()`
      - 객체의 기본 정렬 기준(Comparable)에 따라 오름차순으로 정렬
      - `stream.sorted()`
    - `sorted(Comparator<T>)`
      - 지정된 `Comparator` 기준으로 정렬
      - `stream.sorted(Comparator.reverseOrder())`
- 변환
  - 스트림의 요소를 특정 형태로 변환
    - `map(Function<T, R>)`
      - 요소를 지정된 함수에 따라 다른 타입이나 값으로 변환 (1:1 매핑)
      - `stream.map(String::toUpperCase)`
    - `flatMap(Function<T, Stream<R>>)`
      - 스트림의 각 요소를 다른 스트림으로 변환한 후 모든 스트림을 하나의 스트림으로 평탄화(Flattening) (1:N 매핑)
      - `stream.flatMap(list -> list.stream())`
    - `mapToInt()`, `mapToLong()`, `mapToDouble()`
      - 일반 스트림을 기본형(Primitive) 특화 스트림(IntStream, LongStream, DoubleStream)으로 변환
- 조회
  - `peek(Consumer<T>)`
    - 스트림의 요소를 소모하지 않고, 요소에 특정 작업(예: 로깅, 출력)을 수행
    - 연산 중간 결과를 확인할 때 유용
    - `stream.peek(System.out::println)`
- Java 9+
  - `takeWhile(Predicate<T>)`
    - 조건이 true인 동안만 요소를 취하고 false가 되는 순간 스트림을 종료 
    - 정렬된 무한 스트림에서 유용
    - `stream.takeWhile(x -> x < 10)`
  - `dropWhile(Predicate<T>)`
    - 조건이 true인 동안의 요소를 버리고 처음으로 false가 되는 순간부터 나머지 모든 요소를 취함
    - `stream.dropWhile(x -> x < 10)`