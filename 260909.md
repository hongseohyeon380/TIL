### 인덱스 튜닝의 두 가지 핵심 방향
- 인덱스 : 대용량 테이블에서 소량의 데이터를 빠르게 검색하기 위함
- 1. 인덱스 스캔 효율화 튜닝 = 인덱스 트리를 스캔하는 범위 자체를 줄이는 것 (수평적 탐색 범위 최소화)
- 2. 랜덤 액세스 최소화 튜닝 = 인덱스를 읽고 나서 실제 테이블 블록을 찾아가는(ROWID 접근) 횟수를 줄이는 것
- 우선순위 = '랜덤 액세스 최소화 튜닝'이 성능에 미치는 영향이 훨씬 큼 (테이블 방문 비용이 인덱스 스캔 비용보다 압도적으로 비싸기 때문)

### 인덱스 구조 (B-Tree)
- B-Tree (Balanced Tree) = RDBMS에서 가장 범용적으로 사용하는 인덱스 구조 (루트 - 브랜치 - 리프 계층)
- 루트(Root) 및 브랜치(Branch) 노드 = 하위 노드를 찾아가기 위한 네비게이션 역할
  - 인덱스 키 값과 하위 노드를 가리키는 블록 주소(DBA)를 가짐
  - LMC (Leftmost Child) = 각 노드의 가장 왼쪽 자식 노드를 가리키는 포인터로, 첫 번째 키 값보다 작은 값들을 찾아갈 때 사용됨
- 리프(Leaf) 노드 = 실제 데이터의 위치를 가리키는 종착지
  - 인덱스 키 값과 해당 테이블 레코드를 찾아갈 수 있는 물리적 주소(ROWID)를 가짐
  - 키 값 순으로 정렬되어 있으며, 앞뒤 리프 노드끼리 양방향 링크드 리스트(Doubly Linked List)로 연결되어 있음

### 인덱스 수직적 탐색 (Vertical Scan)
- 목적 = 인덱스 스캔을 시작할 '시작점(Starting Point)'을 찾는 과정
- 동작 방식 = 루트 노드에서 시작해 브랜치 노드를 거쳐 리프 노드까지 '위에서 아래로' 탐색함
- 비교 원리 = 찾고자 하는 값과 노드의 키 값을 비교해, 조건에 맞는 하위 노드의 블록 주소(DBA)를 따라 내려감
- 특징 = 수직적 탐색은 조건을 만족하는 '모든' 데이터를 찾는 과정이 아님. 스캔을 시작할 첫 번째 레코드 딱 하나를 단번에 찾아가는 과정임 (랜덤 액세스 I/O 발생)

### 인덱스 수평적 탐색 (Horizontal Scan)
- 목적 = 수직적 탐색으로 찾은 시작점부터 조건에 만족하는 '모든' 데이터를 찾는 과정
- 동작 방식 = 리프 노드의 양방향 링크드 리스트를 타고 '좌에서 우로(또는 우에서 좌로)' 스캔함
- 탐색 종료 = 리프 노드를 순차적으로 읽다가 검색 조건을 벗어나는 값을 만나는 순간 탐색을 멈춤 (Stop Key)
- 테이블 액세스 = 스캔 과정에서 조건에 맞는 인덱스 레코드를 찾을 때마다, 레코드가 가진 ROWID를 이용해 실제 테이블 블록으로 찾아감 (여기서 대량의 테이블 랜덤 액세스 발생 시 성능 저하)