### `java.util.function`
- 일반적으로 자주 쓰이는 함수형 인터페이스를 정의
- java.lang.Runnable : `void run()`
- Supplier<T> : `T get()`
- Consumer<T> : `void accept(T t)`
- Function<T, R> : `R apply(T t)`
- Predicate<T> : `boolean test(T t)` 
- 매개변수 2개면 앞에 `Bi`
- 기본형 데이터 타입을 사용하는 함수형 인터페이스들도 있음
- 람다식끼리 결합하여 새로운 람다식 가능
- Operator 인터페이스 : 매개변수의 타입과 반환타입의 타입이 모두 일치 (`T --apply(T t)--> T`)

### wrapper class 
- 기본형 데이터 타입을 객체로 (객체면 `null` 가능)
- 기본형 -> 객체 타입 : 박싱
- 객체 타입 -> 기본형 : 언박싱
- 박싱 / 언박싱 자동 -> 오토 박싱 / 오토 언박싱
- 내부적으로는 여전히 객체 생성 연산이 일어남 -> 메모리 낭비 , GC 부하 
- GC (Garbage Collection, 가비지 컬렉션) : 사용하지 않는 쓰레기 객체를 메모리에서 자동 삭제하는 작업 

### 메서드 참조
- `Function<String, Integer> f = (String s) -> Integer.parseInt(s);` -> `Function<String, Integer> f = Integer::parseInt;`
- 하나의 메서드만 호출하는 람다식은 `클래스이름::매서드이름` , `참조변수::메서드이름`으로 변경 가능하다
- 람다식이 하나의 메서드를 호출하면서 입력값을 그대로 전달할 때 사용
- 생성자 참조 : new 생성자 생성 할 때
- 임의 객체의 인스턴스 메서드 참조 : 매개변수로 넘어온 객체 자신의 메서드를 호출할 때