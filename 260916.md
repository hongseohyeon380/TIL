### 객체지향 (Object-Oriented)

- 객체: 고유 식별자 / 하나의 독립된 존재 / 일정한 기억장소 보유
  - 상태: 객체가 가질 수 있는 조건 / 속성 값에 의해 정의됨
  - 행위: 객체가 반응할 수 있는 메시지 집합
- 클래스: 공통 속성과 행위(연산 Method)를 갖는 객체들의 집합 / 데이터 추상화 단위
  - 인스턴스: 클래스에 속한 각각의 객체
  - Operation: 클래스의 동작 / 객체에 대해 적용될 메서드 정의
- 추상화: 객체의 공통적인 속성과 기능을 추출하여 정의 / 유연한 설계 및 유지보수 용이함
  - 구성 요소: 추상 클래스 / 인터페이스
- 캡슐화: 데이터와 데이터 처리 함수를 하나로 묶음 / 세부 내용 은폐(정보 은닉) / 외부 접근 제한 / 결합도 낮음 / 재사용 용이함 / 인터페이스 단순함 / 오류 파급효과 낮음
- 상속: 상위 클래스의 속성과 연산을 하위 클래스가 물려받는 것
- 다형성: 하나의 메시지에 각 객체별 고유 특성에 따라 여러 형태의 응답 가능함
- 오버라이딩 (Overriding): 상위 클래스로부터 상속받은 메서드를 하위 클래스에서 재정의 / 메서드 이름 및 매개변수와 반환 타입은 동일해야 함
- 오버로딩 (Overloading): 메서드 이름은 동일하나 매개변수 개수 또는 타입을 다르게 지정함
- 객체지향 설계 5대 원칙 (SOLID)
  - 단일 책임 원칙 (SRP): 모든 클래스 및 객체는 하나의 책임만 가짐 / 완전한 캡슐화
  - 개방 폐쇄 원칙 (OCP): 확장에는 Open 수정에는 Close
  - 리스코프 교체 원칙 (LSP): 상위 클래스의 행동 규약을 하위 클래스가 위반하면 안 됨
  - 인터페이스 분리 원칙 (ISP): 클라이언트가 비사용 메서드에 의존하지 않아야 함
  - 의존성 역전 원칙 (DIP): 의존 관계 수립 시 변화하기 어려운 추상화에 의존함

---

### 상태 기계 (State Machine)

- 상태 기계 개념
  - 상태(State)와 이벤트(Event)를 처리하는 시스템을 모델링하고 분석하는데 사용됨
  - 입력을 받아 일련의 상태를 거치면서 출력을 생성하는 추상적인 기계 모델
  - 가질 수 있는 State 수에 따라 FSM(유한 상태 기계) / ISM(무한 상태 기계)으로 구분됨
  - 한 번에 오직 한 개의 State만을 가지며 Current State는 임의의 주어진 시간 동안의 상태를 의미함
  - 어떠한 Event에 의해 한 상태에서 다른 상태로 변하는 현상을 천이(Transition)라 함
    - 하나의 Input ➔ State Transition 발생
    - FSM은 현재 상태로부터 가능한 천이 상태와 그 천이를 일으키는 조건의 집합으로 정의됨
  - 사용 이유: 안전성 (정해진 Event에 따라서만 System 동작) / 편의성 (설계 단순화 및 문서화 시간 단축)

- 상태 기계 구성 요소 (UML State Diagram)
  - 상태 (State): 시스템이 존재할 수 있는 조건 또는 상황 / 모서리가 둥근 사각형으로 표기함
  - 시작 상태 (Initial State): 객체의 시작을 의미 / 검은색 실선 원 표기
  - 최종 상태 (Final State): 객체의 소멸 또는 프로세스 종료를 의미 / 이중 원 표기
  - 전이 (Transition): 한 상태에서 다른 상태로의 변화 / 화살표 표기
  - 이벤트 (Event / Trigger): 전이를 유발하는 외부 자극 또는 조건
  - 가드 (Guard): 전이가 일어나기 위한 불리언(Boolean) 조건 / `[조건]` 형식으로 표기함
  - 동작 (Action): 전이가 일어날 때 수행되는 작업

- 상태 내 내부 전이 (Internal Transition Activity)
  - entry: 해당 상태에 진입할 때 실행되는 동작
  - do: 해당 상태에 머무르는 동안 지속적으로 실행되는 동작
  - exit: 해당 상태를 탈출할 때 실행되는 동작

- FSM 출력 방식에 따른 분류
  - 밀리 기계 (Mealy Machine): 출력이 현재 상태와 입력값 모두에 의해 결정됨
  - 무어 기계 (Moore Machine): 출력이 오직 현재 상태에 의해서만 결정됨