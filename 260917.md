### 디자인 패턴 (Design Pattern) 
1. 생성 패턴 (Creational Patterns)
- 객체 생성 및 인스턴스화 과정 캡슐화 / 객체 생성 방식 제어 패턴
- Factory Method
  - 객체 생성을 하위 클래스에서 담당하도록 분리함
  - 예시: `CarFactory.createCar()`
- Abstract Factory
  - 구체적인 클래스 지정 없이 연관되거나 의존적인 객체들의 조합을 한 번에 생성함
  - 예시: `GUIFactory.createButton()`
- Singleton
  - 전역에서 하나의 인스턴스만 생성하여 공유함
  - 예시: `Database.getInstance()`
- Prototype
  - 원본 객체를 복제하여 새로운 객체를 생성함
  - 예시: `object.clone()`
- Builder
  - 복잡한 객체 생성 과정을 단계별로 분리하여 구성함
  - 예시: `StringBuilder.append()`

2. 구조 패턴 (Structural Patterns)
- 클래스나 객체를 조합하여 더 큰 구조를 형성하는 패턴
- Adapter
  - 호환성이 맞지 않는 인터페이스를 변환하여 연결함
  - 예시: USB to Ethernet Adapter
- Bridge
  - 구현부와 추상층을 분리하여 독립적으로 확장 가능하게 함
  - 예시: `Shape(Color color)`
- Composite
  - 객체들을 트리 구조로 구성하여 단일 객체와 복합 객체를 동일하게 다룸
  - 예시: 파일 시스템
- Decorator
  - 기존 객체에 동적으로 새로운 기능을 추가함
  - 예시: `BufferedReader(new FileReader())`
- Facade
  - 복잡한 서브시스템에 대해 단순화된 고수준 인터페이스를 제공함
  - 예시: JDBC Connection
- Flyweight
  - 객체 공유를 통해 메모리 사용량을 최적화함
  - 예시: 문자열 풀 (String Pool)
- Proxy
  - 실제 객체에 대한 접근을 제어하기 위해 대리 객체를 사용함
  - 예시: 가상 프록시 / 스마트 프록시

3. 행위 패턴 (Behavioral Patterns)
- 객체 간의 상호작용 및 책임 분배 알고리즘 정의 패턴
- Chain of Responsibility
  - 요청을 처리할 수 있는 객체들이 사슬 형태로 연결되어 요청을 순차 전달함
  - 예시: 이벤트 핸들러 체인
- Command
  - 요청을 객체 형태로 캡슐화하여 재사용 및 취소 가능하게 함
  - 예시: Undo / Redo 기능
- Interpreter
  - 언어의 문법 표현 및 구문 해석을 담당함
  - 예시: SQL 파서
- Iterator
  - 컬렉션 내부 구조를 노출하지 않고 순차적으로 요소에 접근함
  - 예시: Java Iterator
- Mediator
  - 객체 간 직접 통신을 막고 중재자 객체를 통해 상호작용함
  - 예시: 채팅방 시스템
- Memento
  - 객체의 내부 상태를 저장하여 이전 상태로 복원 가능하게 함
  - 예시: 게임 상태 저장
- Observer
  - 한 객체의 상태 변화 발생 시 관찰 대상 객체들에게 자동 통지함
  - 예시: 이벤트 리스너
- State
  - 객체 내부 상태 변화에 따라 동일한 동작을 다르게 수행함
  - 예시: TCP 연결 상태 변화
- Strategy
  - 동일 계열 알고리즘들을 개별 캡슐화하여 동적으로 교체 사용함
  - 예시: 정렬 알고리즘 선택
- Template Method
  - 상위 클래스에서 전체적인 알고리즘 골격을 정의하고 세부 구현은 하위 클래스에서 담당함
  - 예시: 프레임워크 후킹 메서드
- Visitor
  - 객체 구조 변경 없이 새로운 연산을 추가 적용함
  - 예시: XML 파서