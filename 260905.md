### SQL 최적화
- SQL 파싱(파싱 트리 생성 -> Syntax 체크 -> Semantic 체크) -> SQL 최적화 -> 로우 소스 생성
- SQL 옵티마이저 = 사용자가 원하는 직업을 가장 효율적으로 수행할 수 있는 최적의 데이터 액세스 경로를 선택해주는 DBMS의 핵심 엔진 
  - 사용자로부터 전달 받은 쿼리를 수행하는 데 후보군이 될만한 실행계획들을 찾아낸다
  - 데이터 딕셔너리에 미리 수집해 둔 오브젝트 통계 및 시스템 통계정보를 이용해 각 실행계획의 예상비용을 산정한다
  - 최저 비용을 나타내는 실행계획을 선택한다
- 실행계획 (Execution Plan) = SQL 옵티마이저가 생성한 처리 절차를 사용자가 확인할 수 있게 표현 -> 확인 후 실행 경로 변경 가능 
- 옵티마이저 힌트 = 데이터 액세스 경로 변경 가능 (`/*+ <- 주석 기호에 '+' 붙이기*/`)
  - 힌트와 힌트 사이에 ',' 사용 X (힌트 내 인자 나열할 땐 O)
  - 테이블 지정할 때 스키마명까지 명시하면 X
  - FROM절에서 ALIAS 지정했다면 힌트에서도 ALIAS 사용 
  - 최적화 목표, 액세스 방식, 조인순서, 조인방식, 서브쿼리 팩토링, 쿼리 변환, 병렬 처리, 기타..........

### 소프트 파싱 vs 하드 파싱
- 라이브러리 캐시 = SQL 파싱, 최적화, 로우 소스 생성 과정을 거쳐 생성한 내부 프로시저를 반복 재사용할 수 있도록 캐싱해 두는 메모리 공간
- SGA(System Global Area) : 서버 프로세스와 백그라운드 프로세스가 공통으로 액세스 하는 데이터 제어 구조를 캐싱하는 메모리 공간
- SQL문 전달 -> DBMS SQL문 파싱 -> 라이브러리 캐시에 존재하는 지 확인 
  - 존재 O -> 곧바로 실행 **소프트 파싱**
  - 존재 X -> 최적화 및 로우 소스 생성 **하드 파싱**
- SQL 최적화 시 옵티마이저가 사용하는 정보
  - 테이블, 컬럼, 인덱스 구조에 관한 기본 정보
  - 오브젝트 통계 : 테이블 통계, 인덱스 통계, 컬럼 통계
  - 시스템 통계 : CPU 속도, Single Block I/O 속도, Multiblock I/O 속도 등
  - 옵티마이저 관련 파라미터
- -> 복잡하다 -> SQL 최적화 과정은 하드하다...
- -> 라이브러리 캐시가 필요한 이유

### 바인드 변수의 중요성
- SQL은 이름이 없음 -> 전체 SQL 텍스트가 이름 역할을 한다
- 리터럴(상수) 쿼리의 문제 = `WHERE ID = 1`과 `WHERE ID = 2`는 의미상 같지만, 텍스트가 다르기 때문에 라이브러리 캐시에서 서로 다른 독립적인 쿼리로 취급됨
- 하드 파싱 유발 = 동시 접속자가 많은 환경에서 리터럴 쿼리를 남발하면 매번 무거운 하드 파싱이 발생하여 CPU 자원을 고갈시키고 심각한 성능 저하 초래
- 바인드 변수 사용 = `WHERE ID = :id` (또는 `?`) 형태로 변수를 사용하면 파라미터 값만 바뀌므로 동일한 SQL 텍스트로 인식됨
- 소프트 파싱 유도 = 한 번 하드 파싱을 거쳐 캐싱된 실행계획을 다음 번 실행 시부터 그대로 재사용하게 됨 (라이브러리 캐시 히트율 상승)
- 부가적 이점 (보안) = 사용자 입력값을 문자열로 단순 연결(Concatenation)하지 않으므로, 악의적인 쿼리 조작 기법인 SQL 인젝션(SQL Injection)을 원천 차단함