### `static`
- 객체에 소속되지 않고 클래스에 소속
- 클래스에 고정되어 모든 객체가 공통으로 사용하는 정적 멤버를 만들 때 쓰는 키워드
- 객체를 여러 개 만들어도 static 변수는 하나만 메모리에 할당되어 모든 객체가 값을 **공유함**
- `new`로 객체를 만들지 않아도 `클래스이름.변수명` 또는 `클래스이름.메서드명`으로 바로 접근 가능
- 공통으로 사용하는 값(상수)이나 상태를 저장할 때, 또는 객체 상태와 상관없이 동작하는 유틸리티 메서드에 주로 사용

### `@PostConstruct`와 static 필드 초기화
- **스프링 빈 생명주기**
  - 스프링 컨테이너 생성 -> Bean(인스턴스) 생성 -> 의존관계 주입(`@Value`, `@Autowired`) -> **초기화 콜백(`@PostConstruct`)** -> 사용 -> 소멸 콜백 -> 스프링 종료
- `@PostConstruct`는 의존성 주입이 완료된 후 실행되는 초기화 콜백 메서드에 사용됨
- **왜 사용하는가?** `@Value`를 통해 주입받은 설정값을 `static` 필드에 할당하고 싶을 때 사용
  - static 필드와 메서드는 JVM **클래스 로딩 시점**에 이미 메모리에 올라가서 초기화됨 (스프링 빈 생성 전)
  - 반면 `@Value`는 스프링 컨테이너가 Bean 인스턴스를 생성한 후에 객체 레벨에서 값을 주입함
  - 즉, static 필드는 클래스 레벨이기 때문에 스프링이 직접 `@Value`로 값을 주입할 수 없음
- **해결 흐름 (HTTP Header 생성 유틸리티 등 적용 시):**
  1. JVM이 클래스를 로드하면서 static 필드 초기화 완료
  2. Spring IoC 컨테이너가 해당 클래스를 Bean으로 등록하고 인스턴스화
  3. 인스턴스 필드에 `@Value`로 환경변수/설정 데이터 주입
  4. Bean 등록 및 의존성 주입이 끝나는 시점에 `@PostConstruct`가 실행되어, 주입된 인스턴스 필드 값을 static 필드에 재할당 (한 번 더 초기화)
  5. 이후 인스턴스 생성 없이도 `클래스명.메서드()` 형태로 간결하고 편하게 헤더 생성 등의 기능 사용 가능