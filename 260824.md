### Stream
- 많은 수 데이터를 컬렉션, 배열 등으로 관리 -> 코드 길어짐, 재사용성 X, 데이터 소스마다 다른 방식
- `Stream` 에서 데이터 소스를 추상화하여 자주 사용하는 메서드를 정의
  - 스트림은 데이터 소스를 변경하지 않고 읽기만 한다 (결과를 리스트 등에 담기는 가능)
  - 스트림은 일회용이다
    - `stream.sorted().forEach(System.out::println)` 이후 `int num = stream.count()` 불가
    - 작업을 내부 반복문으로 처리
  - stream operation : 중간 연산 / 최종 연산
    - 최종 연산이 수행되어야 스트림 요소가 중간 연산을 거쳐 최종 연산에서 소모된다 
  - `parallel()` 병렬 처리 
    - 병렬처리가 빠른 결과를 가져오는 것은 아님
      - 오버헤드, 자료구조 특성, context switching, 공유 자원 접근 문제, 공용 스레드 풀 사용 문제 생각 필요
- Collectors -> DB에서 꺼내온 List 형태의 데이터를 API 규격에 맞추거나 다른 로직에 태우기 위해 Map 형태로 변환할 때

```java
// List -> Map 변환
Map<Long, Member> memberMap = members.stream()
    .collect(Collectors.toMap(Member::getId, member -> member));
```

```java
// 속성 기준으로 그룹화
Map<Role, List<Member>> membersByRole = members.stream()
    .collect(Collectors.groupingBy(Member::getRole));

// 그룹별 카운트
Map<Role, Long> countByRole = members.stream()
    .collect(Collectors.groupingBy(Member::getRole, Collectors.counting()));

// 그룹별 특정 속성만 리스트로 추출
Map<Role, List<String>> namesByRole = members.stream()
    .collect(Collectors.groupingBy(
        Member::getRole,
        Collectors.mapping(Member::getName, Collectors.toList())
    ));
```

```java
// 참/거짓 두 그룹으로 분리
Map<Boolean, List<Member>> partitionedMembers = members.stream()
    .collect(Collectors.partitioningBy(Member::isHasLoggedIn));
```

### etc..
- static import : 
  - 다른 class의 상수를 사용할 때 정적 임포트로 사용 
  - 이에 따라 class 이름 안 붙이고 상수처럼 사용 가능
  - 남발하면 어디 class의 상수인지, 변수와 이름 혼동 등 문제 발생할 수 있으니 주의
- 유휴만료 vs 절대만료
  - 유휴만료 : 마지막 사용 혹은 접근 시점 기점으로 만료시간 계산 -> 사용자 계속 사용 시 만료시간 연장 
  - 절대만료 : 생성 시점 기점으로 만료시간 계산 
  - RTR(Refresh Token Rotation) : refresh token 1회 사용 시 만료시키고 다른 refresh token 발급 -> 탈취된 refresh token도 만료된 중복 요청이라 인지하고 모든 세션 강제 만료 가능