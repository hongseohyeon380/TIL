### Function Calling
- LLM이 직접 외부 데이터베이스, API, 시스템 등에 접근하여 정보를 검색하거나 특정 조치를 수행할 수 있도록 연결해 주는 기능
- 활용 범위
  - 정보 검색 : DB, Web API, 파일 시스템, 웹 검색 엔진 등 외부 소스에서 데이터를 조회 (RAG 고도화)
  - 조치 수행 : 이메일 발송, DB 레코드 생성/수정, 결제 승인, 슬랙 알림 등 액션 처리 (챗봇/AI Agent 구현)
- 동작 순서
```
[사용자 요청] ➔ [1. 도구 정의 포함 전송] ➔ [LLM]
                                           │
[최종 응답 생성] ⬅ [4. 결과 재전송] ⬅ [2. 도구 호출 요청 (JSON)]
                                           │
                                  [3. 애플리케이션 메서드 실행]
```
- 도구 정의 포함 : 채팅 요청 시 사용할 수 있는 도구의 정의(이름, 설명, 매개변수 스키마)를 LLM에 전달
- 도구 호출 요청 : LLM이 도구 사용이 필요하다고 판단하면 도구 이름과 매개변수를 담아 응답
- 애플리케이션 실행 : 애플리케이션이 전달받은 매개변수로 해당 자바 메서드를 실행
- 결과 처리 및 반환 : 실행 결과를 후처리하여 다시 LLM에 전달
- 최종 응답 생성 : LLM이 도구 실행 결과를 컨텍스트로 활용하여 최종 답변 생성

- `@Tool`
  - 메서드를 LLM이 호출할 수 있는 도구로 등록
  - description : 도구의 역할과 사용 시점을 설명하는 문구 (구체적일수록 LLM의 인식률 상승).
  - name : 도구의 고유 이름 (기본값: 메서드명)
  - returnDirect : 실행 결과를 AI를 거치지 않고 사용자에게 직접 반환할지 여부 (기본값: false)
  - resultConverter : AI에 전달할 결과값을 변환하는 DTO 컨버터 지정
- `@ToolParam`
  - 도구 메서드의 파라미터 스펙을 정의
  - description : 파라미터의 의미 및 형식 가이드 (예: yyyy-MM-dd HH:mm:ss)
  - required : 필수 여부 (기본값: true)