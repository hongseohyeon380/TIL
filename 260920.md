### 데이터베이스 정규화 (Normalization) 

- 데이터 중복을 최소화하고 이상 현상을 방지하기 위한 데이터베이스 설계 기법
- 데이터 구조의 안정성 향상 및 유지보수 용이성 확보

- 이상 현상 (Anomaly) : 정규화를 하지 않았을 때 발생할 수 있는 데이터 불일치 및 오류 현상
  - 삽입 이상 (Insertion Anomaly): 데이터 삽입 시 불필요한 정보까지 강제로 함께 입력해야 하는 현상
  - 삭제 이상 (Deletion Anomaly): 원하는 데이터 삭제 시 필요한 다른 정보까지 연쇄적으로 삭제되는 현상
  - 갱신 이상 (Update Anomaly): 일부 데이터 수정 시 데이터 간 불일치 및 모순이 발생하는 현상

- 제1정규형 (1NF): 도메인 원자값
  - 테이블의 모든 컬럼이 **원자값(Atomic Value)**만을 가져야 함 (다중 값, 반복 그룹 제거)
  - 한 칸에는 무조건 값 하나만 넣기
  - 한 셀에 `"축구, 영화, 음악"`처럼 여러 값이 들어간 경우, 데이터 하나당 하나의 행을 가지도록 분리
- 제2정규형 (2NF): 부분 함수적 종속성 제거
  - 기본키가 복합키(2개 이상의 컬럼)일 때, 기본키 전체에 종속되지 않는 **부분 함수적 종속성**을 제거 (완전 함수적 종속 만족)
  - 기본키 일부에만 얹혀사는 일반 컬럼 떼어내기
  - `[학번 + 과목코드]`가 기본키일 때, `과목명`은 `과목코드`에만 종속되므로 별도의 과목 테이블로 분리
- 제3정규형 (3NF): 이행적 함수적 종속성 제거
  - `A -> B`이고 `B -> C`일 때 `A -> C`가 성립하는 **이행적 함수 종속성**을 제거 (일반 컬럼 간의 종속 관계 분리)
  - 일반 컬럼끼리 엮여 있는 꼬리물기 관계 떼어내기
  - `학번(A) -> 부서코드(B) -> 부서위치(C)` 관계에서, 일반 컬럼인 `부서코드`에 종속된 `부서위치`를 별도 테이블로 분리
- 보이스-코드 정규형 (BCNF): 결정자 = 후보키
  - 모든 결정자(Determinant)가 후보키(Candidate Key)가 되도록 설정 (강한 제3정규형)
  - 남을 결정짓는 컬럼은 무조건 대표(후보키)여야 함
  - 기본키가 아닌 일반 컬럼이 다른 컬럼을 결정하는 주도권을 쥐고 있다면, 해당 결정자를 후보키로 만들 수 있도록 테이블 분리 (예: `[학번 + 과목] -> 교수` 관계에서 `교수 -> 과목` 종속 시 분리)
- 제4정규형 (4NF): 다치 종속성 제거
  - 하나의 테이블에 독립적인 다중 값 관계가 존재하는 **다치 종속성(Multi-valued Dependency)** 제거
  - 서로 상관없는 여러 다중 값의 얽힘 풀어주기
  - `[개발자 - 보유기술 - 보유자격증]` 관계에서 기술과 자격증은 서로 무관하므로 `[개발자 - 기술]`, `[개발자 - 자격증]`으로 분리
- 제5정규형 (5NF): 조인 종속성 제거
  - 테이블을 분할했다가 재조합(조인)할 때 손실이나 가짜 데이터가 생기지 않도록 **조인 종속성(Join Dependency)** 제거
  - 쪼갰다 합쳤을 때 헛소리(가짜 데이터) 안 나오게 만들기
  - 원래 관계의 모든 조인 종속이 후보키를 통해서만 성립하도록 분할하여 복원 시 유령 데이터 발생 방지
- 반정규화 (Denormalization) : 시스템 성능 향상, 조회 단순화를 위해 정규화된 데이터 모델을 의도적으로 통합, 중복, 분할하는 기법
  - 테이블 분할: 수평 분할(행 단위) / 수직 분할(컬럼 단위)
  - 테이블 통합: 1:1 관계 통합 / 1:N 관계 통합 / 슈퍼-서브타입 통합
  - 중복 속성 추가: 자주 조회되거나 집계되는 컬럼을 중복하여 추가
- 주의점: 무분별하게 적용 시 데이터 일관성/무결성이 깨지고 유지보수가 어려워짐