### 람다식
- 익명 함수 -> 메서드를 하나의 식으로 표현
- 이름 없음. 매개변수의 타입, `return`문 생략 가능  
- 메서드 내부 문장이 식으로 표현됨 -> ; 없이 **(실행문이 1개인 경우)**
- 메서드 -> 변수 

```
type method (parameter) {
    statements;
    ...
}

(parameter) -> expression

(parameter) -> { expression; }

```

### 함수형 인터페이스
- 참조변수 : 실제 값 X 객체의 참조값 O
- 익명 객체 : 클래스 이름이 없는 1회용 객체 (내부에서 1회성으로 사용)
- **함수형 인터페이스를 구현한** 익명 객체를 람다식으로 대체 가능
- `@FunctionalInterface` 
- 함수형 인터페이스 : 람다식을 다루기 위한 인터페이스 
- 오직 1개의 추상 메서드만 정의 -> 람다식과 1:1 