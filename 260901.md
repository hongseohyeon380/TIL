### 프로세스와 쓰레드
- 프로세스 = 실행중인 프로그램 (실행에 필요한 자원을 할당받아 프로세스가 된다)
- 프로세스 = 수행하는데 필요한 데이터 + 자원 (메모리) + 쓰레드
- 쓰레드 = 프로세스의 자원을 이용해서 실제로 작업을 수행한다
- 멀티태스킹 = 다중 작업 -> 여러 개의 프로세스 동시에 실행 가능
- 멀티쓰레딩 = 하나의 프로세스 내에서 여러 쓰레드가 동시에 작업 수행
  - 장점
    - CPU의 사용률 향상
    - 자원 효율적 사용
    - 사용자에 대한 응답성 향상
    - 작업이 분리되어 코드 간결
  - 단점
    - 자원 공유 -> 동기화, 교착상태 문제 고려해야함

### 쓰레드 구현
- 쓰레드를 구현 = 쓰레드를 통해 작업하고자 하는 내용으로 `run()`의 몸통`{}`를 채우는 것
- Thread 클래스 상속
    ```java
    classs MyThread extends Thread {
        public void run(){}
        }
    ```
    - 자손 클래스에서 조상인 Thread 클래스의 메서드를 직접 호출할 수 있음
- Runnable 인터페이스 구현
    ```java
    classs MyThread implements Runnable {
        public void run(){}
        }
    ```
    - run()만 정의되어있는 간단한 인터페이스 
    - Runnable 인터페이스 구현 후 인스턴스 생성, 이 인스턴스를 Thread 클래스의 생성자의 매개변수로 제공해야 함
    - Thread 클래스의 static 메서드인 `currentThread()`를 호출하여 쓰레드에 대한 참조를 얻어 와야만 호출 가능

### 쓰레드 실행
- 쓰레드 생성 -> 자동 실행 X
- `start()`를 호출해야 쓰레드가 실행 (단, 바로 실행 X)
- 실행 순서는 OS의 스케쥴러에 의해 결정 
- 한번 실행이 종료된 쓰레드는 다시 실행 불가
  - 다시 생성 후 실행시켜야 함
  - 하나의 쓰레드에 대해 `start()` 2번 이상 -> `IllegalThreadStateException`