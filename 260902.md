### Thread `start()`와 `run()`
- 쓰레드 실행 = `start()` O `run()` X
- main 메서드에서 `run()`을 호출하는 것은 생성된 쓰레드를 실행시키는 것이 아니라 클래스에 선언된 메서드를 호출하는 것
- `start()`는 새로운 쓰레드가 작업을 실행하는데 필요한 호출 스택(call stack)을 생성한 다음에 `run()`을 호출해서 생성된 호출 스택에 `run()`이 첫 번째로 올라가게 함
- 모든 쓰레드는 독립적인 작업을 수행하기 위해 자신만의 호출 스택을 필요로 함 
- 새로운 쓰레드 생성 및 실행 -> 새로운 호출 스택 생성, 쓰레드 종료 -> 작업에 사용된 호출 스택 소멸
- 스케쥴러 = 실행 대기중인 쓰레드들의 우선순위를 고려해서 실행순서와 실행시간을 결정
- 각 쓰레드들은 작성된 스케줄에 따라 자신의 순서가 되면 지정된 시간동안 작업 수행
- 주어진 시간동안 작업 마치지 못함 -> 대기 상태 
- main 쓰레드 = main 메서드의 작업을 수행하는 쓰레드

### 일반 쓰레드(User Thread)와 데몬 쓰레드(Daemon Thread)
- 데몬 쓰레드 = 다른 일반 쓰레드(User Thread)의 작업을 돕는 보조적인 역할을 수행하는 쓰레드
- 데몬 쓰레드의 종료 = 실행 중인 일반 쓰레드가 모두 종료되면 데몬 쓰레드는 강제적으로 자동 종료됨 (보조 역할이므로 더 이상 존재할 의미가 없기 때문)
- 대표적인 예 = 가비지 컬렉터(GC), 워드프로세서의 자동저장, 화면 자동갱신 등
- 구현 방식 = 주로 무한루프와 조건문을 이용해 대기하고 있다가 특정 조건이 만족될 때만 작업을 수행하도록 작성
- 데몬 쓰레드 설정 = 쓰레드 실행(`start()`) 전에 `setDaemon(true)`를 호출해서 설정해야 함
- 주의사항 = `start()`가 호출된 이후에 `setDaemon(true)`를 호출하면 `IllegalThreadStateException` 예외 발생
- 상속성 = 데몬 쓰레드가 생성한 새로운 쓰레드는 기본적으로 데몬 쓰레드가 됨
- 일반 쓰레드(User Thread) = 메인 쓰레드(main thread)를 포함하여 우리가 기본적으로 생성하는 독립적인 작업을 수행하는 쓰레드
- 프로그램 종료 조건 = 실행 중인 일반 쓰레드(User Thread)가 하나도 없을 때 프로그램이 완전히 종료됨 (데몬 쓰레드가 아직 실행 중이더라도 무관함)