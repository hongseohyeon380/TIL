### HTTP 처리 흐름 (Thread-per-request)
- 클라이언트의 HTTP 요청은 내장 톰캣(Tomcat)의 Connector가 수신하여 파싱
- 톰캣은 스레드 풀(Thread Pool)에서 유휴 워커 스레드 1개를 할당
- 이 스레드가 DispatcherServlet 진입부터 DB I/O, JSON 직렬화까지 전 과정을 동기/블로킹(Blocking) 방식으로 처리
- HTTP 응답이 완료되어야만 스레드가 다시 풀로 반환 (I/O 작업 중엔 스레드가 점유 상태)

### Spring Boot 구동 방식의 이점
- 전통적 방식: 외부 WAS(Tomcat) 프로세스 1개에 여러 `.war` 앱을 호스
- 앱 간 메모리 격리 X
- Spring Boot: `spring-boot-starter-web`이 톰캣을 라이브러리로 내장하여 앱 구동 시 톰캣 객체를 띄움
- 1 Container = 1 Process = 1 JVM 원칙이 성립
- 운영 관점: 특히 쿠버네티스(Kubernetes) 환경에서 HPA(Horizontal Pod Autoscaler)로 파드(Pod) 오토스케일링을 하거나 부하 테스트를 진행할 때 단일 프로세스 구조라 메모리 Limit 설정과 스케일 아웃이 직관적이고 깔끔함

### DispatcherServlet (Front Controller)
- 모든 HTTP 요청을 단일 진입점에서 가로채어 처리
- 요청 처리 파이프라인
  - HandlerMapping: HTTP Request(URI, Method)를 분석해 처리할 @Controller 메서드를 찾음
  - HandlerAdapter: HTTP 페이로드를 컨트롤러의 파라미터 타입에 맞게 데이터 바인딩
  - 비즈니스 로직: 개발자가 작성한 서비스 코드(Service, Repository) 실행
  - HttpMessageConverter: 반환된 객체(DTO)를 JSON 등으로 직렬화하여 HTTP Response Body에 기록
- 프레임워크가 공통 웹 파이프라인을 전담하므로, 개발자는 순수 비즈니스 로직(POJO) 구현에만 집중할 수 있음

### 아키텍처 확장 (WebFlux)
- 기존 동기 방식은 트래픽 급증 시 스레드 풀이 고갈되어 병목이 발생할 수 있음
- 데이터 파이프라인(Kafka) 연동이나 대용량 검색 엔진(Elasticsearch) 쿼리처럼 비동기 I/O 처리가 중요한 도메인에서는 스레드 블로킹 없이 Event-Loop 기반으로 동작하는 Spring WebFlux를 대안으로 도입