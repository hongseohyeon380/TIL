### 쇼트서킷
- **최종 연산이 수행되어야 스트림 요소가 중간 연산을 거쳐 최종 연산에서 소모된다**가 무슨말이냐
- 데이터 가공 순서는 C->A->B가 되는게 아니라 A->B->C가 맞음
- C가 실행될 때 A, B 연산 통과 하여 C도달 
- A나 B를 통과하지 못한 데이터들은 C에 도달 불가
  

### 의존성 주입
- 생성자 주입 : 객체가 만들어질 때 딱 한번 실행되는 초기값 세팅 과정 -> 객체 생성 시에 의존성 주입(필요한 기능 주입, 스프링 빈 생성 시점에 동작)
- 필드 주입 : 객체가 가지는 고유한 상태, 데이터 저장 공간 (클래스 내 선언 변수) -> 객체 생성 후 필요할 때 의존성 주입
- 생성자 주입을 써야함
  - 불변성 보장 : 필드에 `final` 키워드 선언 불가, 생성자는 객체 생성 시점에 딱 한번 주입되므로 `final`로 선언 가능 -> 객체 불변성 보장 + 런타임에 의존성이 임의로 조작되는것 차단
  - 순수 자바코드 만으로 단위 테스트 가능 (테스트 코드 작성 시 Spring 컨테이너 안띄우고도 가능)
  - 순환 참조 방지 : 필드 주입은 순환 참조 시 런타임 에러 발생, 생성자 주입은 서버 구동 시 에러 발생 (`BeanCurrentlyInCreationException`)
  

### @Value
- properties에 보관되어있는 값을 가져오는 역할
- 단일 값을 주입받기 위해서 사용 
- @Component 통해 객체를 스프링 빈으로 등록
- @Value는 스프링 빈으로 등록 하고 의존관계 주입 할 때 동작 (런타임, 생성자 호출 시점 이후)
- 따라서 @Component로 등록 안하면 @Value 값도 안들어온다
- 상황에 따라서 적절한 주입 방식을 사용해야 함 (생성자 주입, 필드 주입 등등)