### Table Full Scan vs Index Range Scan
- 테이블에 저장된 데이터를 읽는 두 가지 방법 
  - Table Full Scan : 테이블에 할당된 전체 블록을 스캔하여 읽기
  - Index Range Scan : 인덱스를 통해 조건에 맞는 일정량만 스캔하고, 얻어낸 ROWID(데이터의 물리적 디스크 주소)로 테이블 레코드를 찾아가는 방식
- Table Full Scan은 무조건 나쁘다는 식의 실행계획 분석은 튜닝에 큰 도움이 되지 않음
  - 대량의 데이터를 처리하는 집계용 SQL이나 야간 배치 프로그램에서는 인덱스보다 풀 스캔이 압도적으로 유리함
  - 조인 시에도 대량의 데이터를 다룰 때는 테이블 풀 스캔 + Hash Join 조합이 최적의 성능을 냄
- Table Full Scan의 특징 = 시퀀셜 액세스와 Multi Block I/O 방식으로 디스크 블록을 읽음
  - 캐시에서 못 찾으면 I/O Call을 통해 인접한 수십~수백 개 익스텐트 블록을 한 번에 퍼 올림
  - 스캔 과정에서 각 블록을 딱 한 번만 읽으므로 대량 데이터 스캔에 매우 효율적임
  - 아주 소량의 데이터를 찾기 위해 거대한 테이블 전체를 읽는 것은 명백한 비효율
- Index Range Scan의 특징 = 랜덤 액세스와 Single Block I/O 방식으로 디스크 블록을 읽음
  - 레코드 하나를 찾아가기 위해 매번 디스크와 메모리를 오가는 I/O Call 발생
  - 여러 레코드가 같은 테이블 블록에 흩어져 있을 경우, 읽었던 테이블 블록을 여러 번 반복해서 다시 찾아가 읽는 비효율 발생
  - 통상적으로 전체 레코드의 10~20% 이상을 읽을 때는 인덱스 스캔의 랜덤 I/O 비용이 풀 스캔의 시퀀셜 I/O 비용을 압도하여 더 느려짐

### 캐시 탐색 메커니즘과 버퍼 Lock
- 메모리 경유 = Direct Path I/O를 제외한 모든 블록 I/O는 무조건 DB 버퍼 캐시를 먼저 경유함
  - 인덱스 루트 블록 -> 브랜치 블록 -> 리프 블록 -> 테이블 블록을 찾아갈 때 매번 캐시부터 탐색
  - 테이블 Full Scan 시에도 읽어들인 블록은 캐시 메모리에 먼저 올라감
- 해시(Hash) 알고리즘 탐색 = 메모리에서 블록을 찾을 때는 데이터 블록 주소(DBA)를 해시 함수에 입력해 해시 버킷(Hash Bucket)을 먼저 찾아감
- 캐시 버퍼 체인 (Cache Buffer Chain) = 같은 버킷에 할당된 블록들은 양방향 링크드 리스트 형태의 체인으로 연결되어 있으며, 이 체인을 순차적으로 스캔해 원하는 블록을 찾음
- 래치(Latch) 획득 = 여러 프로세스가 동시에 체인을 엉망으로 만드는 것을 막기 위해, 체인을 탐색하려면 먼저 자물쇠 역할인 래치(Cache Buffer Chain Latch)를 획득해야 함
- 버퍼 Lock (Buffer Pinning) = 체인에서 원하는 블록을 찾았다면, 해당 블록을 읽거나 쓰기 위해 블록 자체에 버퍼 Lock(또는 Pin)을 걸어야 함
  - Lock의 목적 = 내가 메모리의 특정 블록을 읽고 있는 동안 다른 프로세스가 그 블록의 데이터를 변경하거나 메모리에서 밀어내지 못하도록 정합성을 보호하기 위함
- 버퍼 캐시(메모리)에서 데이터를 읽는 것은 디스크보다 빠르지만, 래치와 Lock을 획득하고 해제하는 과정이 수반되므로 결코 공짜가 아님
- 논리적 I/O(메모리 읽기 횟수)가 무한정 늘어나면 래치와 버퍼 Lock을 얻기 위한 줄서기(Wait Event 경합)가 발생해 CPU가 멈춤. 따라서 인덱스를 최적화해 논리적 I/O 자체를 획기적으로 줄이는 것이 중요