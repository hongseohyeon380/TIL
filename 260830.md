### 폼 로그인 인증
```java
httpSecurity.formLogin(
    Customizer.withDefaults()
)
```
- SecurityContextHolderFilter: 한번 로그인한 사용자가 다시 로그인하지 않아도 인증된 상태를 유지할 수 있도록 함
- LogoutFilter: 로그아웃 시 로그인한 사용자의 정보를 정리하는 역할 수행
- UsernamePasswordAuthenticationFilter: 로그인을 시도하면 사용자 인증 수행
- DefaultLoginPageGeneratingFilter: 기본 로그인 페이지 만들어 제공
- DefaultLogoutPageGeneratingFilter: 기본 로그아웃 페이지 만들어 제공
- 폼 로그인 인증에서는 UsernamePasswordAuthenticationFilter 객체가 사용됨
  - 인증에 필요한 정보를 HttpServletRequest 인스턴스로부터 전달받아 미인증 Authentication 인스턴스를 생성
    - HttpServletRequest 인스턴스로부터 아이디와 비밀번호를 받음
    - 생성된 Authentication 인스턴스는 UsernamePasswordAuthenticationToken 객체
  - Authentication 인스턴스를 인증 매니저에게 전달하면서 인증 작업 위임
- 실행 흐름
  - UsernamePasswordAuthenticationFilter 객체가 요청을 받으면 ProviderManager 객체에 인증 작업 위임
    - 인증에 필요한 정보는 UsernamePasswordAuthenticationToken 객체에 담아 전달
  - ProviderManager 객체는 해당 인증 작업을 처리할 수 있는 인증 제공자를 찾아 인증 작업 위임
  - DaoAuthenticationProvider 객체는 사용자 정보 조회
    - UserDetailsService 인스턴스가 사용자 정보 조회에 대한 책임을 맡음
  - DaoAuthenticationProvider 객체는 조회한 사용자 정보로 인증 작업 수행
    - 인증이 성공하면 사용자 정보를 새로운 UsernamePasswordAuthenticationToken 객체에 담아 반환

### 리멤버-미 인증
- 자동 로그인 (편의성 <-> 보안 위협 트레이드 오프)
- 리멤버-미 인증 처리를 위해 RememberMeAuthenticationFilter 객체가 사용 됨
```java
httpSecurity.rememberMe(
    Customizer.withDefaults()
)
```
- 실행 흐름
- RememberMeServices 인스턴스에 자동 로그인을 요청
- RememberMeServices 인스턴스는 요청, 응답 정보를 바탕으로 자동 로그인을 수행한 후 Authentication 인스턴스를 반환
- 자동 로그인 성공 여부 확인
  - Authentication 인스턴스가 null이 아닌 경우 성공한 것으로 간주 -> 인증 매니저에게 인증 작업 요청
  - Authentication 인스턴스가 null인 경우 자동로그인이 실패한 것으로 간주 -> 다음 필터 실행
- 인증 매니저에게 인증 작업 요청
- 인증 매니저가 Authentication 인스턴스를 반환 (인증된 사용자 정보를 담고 있음)
- 인증 성공 여부 확인
  - 예외가 발생하지 않은 경우 성공한 것으로 간주
    - 빈 SecurityContext 인스턴스를 만들어 내부에 Authentication 인스턴스를 저장
    - SecurityContext 인스턴스를 SecurityContextHolder 클래서에 보관
    - SecurityContextRepository 인스턴스를 통해 인증 정보를 별도 저장서에 보관
    - 인증 성공에 대한 이벤트 발행
    - 인증 성공 핸들러가 부가적인 작업 수행
  - 예외가 발생한 경우 실패한 것으로 간주
    - 리멤버-미 정보 정리
    - 인증 실패 핸들러가 부가적인 작업 수행
- 사용자 인증이 완료되는 시점에 해당 사용자 정보를 저장 (쿠키나 DB)
- 리멤버-미 인증 작업 만족 조건 
  - 리멤버-미 기능을 활성화한 상태로 로그인을 수행했던 사용자가 다시 로그인을 수행
  - 최초 로그인 세션은 만료되었지만 리멤버-미 토큰 만료시간이 지나지 않은 시점에 로그인을 수행
  - RememberMeAuthenticationFilter 객체 이전에 사용자 인증이 이뤄지지 않아야 함